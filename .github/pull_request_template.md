## 🎨 UI Token Compliance

### Layout & Surfaces
- [ ] No `bg-white` / `text-gray-*` / `border-gray-*` usage
- [ ] Uses `bg-background` for page roots
- [ ] Uses `bg-surface` for cards and sections

### Actions
- [ ] Uses `bg-primary` for main CTAs only

### Status & Semantics
- [ ] Uses `sx-success` / `sx-danger` / `sx-warning` for status meaning
- [ ] Tenant colors are not used for status meaning

### Forms & Tables
- [ ] Inputs/selects use shared UI primitives
- [ ] Tables are wrapped in surface containers

---

## ♿ Accessibility
- [ ] Focus ring behavior unchanged
- [ ] Error states unchanged
